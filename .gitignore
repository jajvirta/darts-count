node_modules
.vscode/
deploy.env

# ad-hoc diagnostic dumps — may contain bucket/origin/distribution identifiers
behaviors.txt
curl-output.txt
diagnose.txt

# stray non-project items that live in this dir — NEVER commit to this public repo
# (login-api is a separate internal git repo; the .gz is a local CLI binary)
/login-api/
yle_*.gz
