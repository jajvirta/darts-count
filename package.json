{
  "name": "darts-count-practice",
  "version": "1.0.0",
  "description": "Practice counting points and checkouts for darts — static site (S3 + CloudFront)",
  "private": true,
  "scripts": {
    "dev": "npx --yes serve public",
    "deploy": "./deploy.sh"
  }
}
